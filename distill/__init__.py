"""Black-box distillation toolkit for a faster travel-agent student model.

Phases 0–3 build datasets. Phase 4+ (training / export) are gated — scripts
exist but must be launched explicitly by the user.
"""

from __future__ import annotations

__all__ = ["__version__"]
__version__ = "0.1.0"
